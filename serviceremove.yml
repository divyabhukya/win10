---
- name: Remove  windows_exporter service
  hosts: 172.16.0.122  # Replace with the target host or group of hosts
  tasks:
    - name: Ensure  windows_exporter service is absent
      win_service:
        name:   windows_exporter
        state: absent

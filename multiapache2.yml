---
- name: Install Apache HTTP Server
  hosts: all
  become: true
  tasks:
    - name: Install Apache2 package
      apt:
        name: apache2
        state: present

    - name: Ensure Apache2 service is running
      service:
        name: apache2
        state: started
        enabled: true
